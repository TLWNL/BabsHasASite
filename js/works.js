function loadImage() {
    /**
     *  images --> Fotos --> [years] --> [images]
     *
     *  from 2014 to 2024:
     *      if there exists a folder name with the same name as the year
     *          select it
     *  for every picture in that folder
     *  create a new tag and inject it into the HTML.
     */
}

